<?php 

    // include_once 'config.php';

    // $query = mysqli_query($connect,"SELECT * FROM eligibility");

    // $row = mysqli_fetch_assoc($query);

    // $data_array = array($row['core_one']=>$row['mark_one'],$row['core_two']=>$row['mark_two'],$row['core_three']=>['mark_three'],$row['core_four']=>$row['mark_four']);



 ?>

 